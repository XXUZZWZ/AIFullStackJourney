# react 面试基础考点

- JSX map ？ 唯一值

  - todos 响应式状态改变了，界面更新。

    - ()=>{ } map 新数组，重新渲染
    - 热更新
    - 只有第一个改变了，只热更新第一项？？？
    - 旧状态 新状态 内存
    - diff 比较不同点

    - 基于索引
    - 默认基于索引比较
    - 为什么不能用 index 作为 key
      - 数组 item 发生改变时，浪费更新 dom 的资源
      - 数组开始插入一个新元素到数组头部，全部节点 index 都变化了,diff 结果是全部节点更新了。
