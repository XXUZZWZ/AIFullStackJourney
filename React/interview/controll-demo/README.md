# 受控组件和非受控组件

## 用于 表单 收集用户数据

- 受 state 状态控制
  value={state} onChange={(e)=>setState(e.target.value)}
  性能开销(使用防抖节流来减少实时更新的性能开销)
  用于：要实时检查表单，比如检查邮件地址是否存在@()
- 另一种 收集用户输入的方法
- 非受 state 状态控制
  性能开销更小
  用于： 交互性不强的，比如上传图片，文件
