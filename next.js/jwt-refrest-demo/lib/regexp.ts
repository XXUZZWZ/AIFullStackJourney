const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
// 这个正则的意思： 邮箱必须要有字符，在@前一定要有字符，在@后一定要有字符，在@后一定要有.，在.后一定要有字符
const passwordRegex = /^(?![a-zA-Z]+$)(?![A-Z0-9]+$)(?![A-Z\W_!@#$%^&*`~()-+=]+$)(?![a-z0-9]+$)(?![a-z\W_!@#$%^&*`~()-+=]+$)(?![0-9\W_!@#$%^&*`~()-+=]+$)[a-zA-Z0-9\W_!@#$%^&*`~()-+=]/;
// 这个正则的意思： 密码必须要有8个字符,且不能全为数字,且不能全为字母,且不能全为符号
// 对密码进行判断 密码的格式是否正确
export { emailRegex, passwordRegex };