# prompt 写死了，有什么问题

- AI 工程角度
- PromptTemplate 是 AI 项目的一个模块
- es6 的模板字符串 可以方便的插入变量
- Agent 赛道里面 code Agent 既定成功
- Cursor Claude Code subAgent
- 写文档的 agent ,话界面的 agent (stitch),原型 agent figma
- 写代码的 agent
- 测试 agent
- 上线 agent

## PromptTemplate

PromptTemplate 是一个模板 把 prompt 抽象为一个带变量的模板，可以在运行时填充不同的参数，从而生成动态 prompt 更好复用。

- 创建一个 PromptTemplate 对象，并传入模板字符串和参数列表。
