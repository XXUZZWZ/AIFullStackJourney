// function filter(array) {
//   return array.filter((item) => {
//     return item > 2;
//   });
// }

// console.log(filter([2, 3, 4, 7, 1, 4]));

//  不能优化 如果 可以是有序的就可以

// 如果是有序数组的话就要 二分

const arr = [1, 2, 3, 5, 6, 8];

const index = arr.findIndex((n) => num > 2);

function filter() {}
