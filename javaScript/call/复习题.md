# Call/Apply/Bind 复习题

## 一、基础概念题

### 1. 选择题

**1.1 关于 call、apply、bind 的说法，错误的是：**
A. 三者都是 Function.prototype 上的方法
B. 三者都能改变函数执行时的 this 指向
C. call 和 apply 的区别在于参数传递方式不同
D. bind 返回的函数会立即执行

**1.2 以下哪个方法会立即执行函数？** 

A. call()
B. apply()
C. bind()
**D. A和B**

**1.3 当 call 的第一个参数为 null 时，在非严格模式下 this 指向：**
A. null
B. undefined
**C. window**
D. 报错

### 2. 判断题

**2.1 call 和 apply 可以互换使用，只是参数传递方式不同。** (对 )

**2.2 bind 方法返回的是原函数的一个副本，与原函数完全独立。** ( 对)

**2.3 在严格模式下，call(null) 会让 this 指向 window。** ( 错)

**2.4 任何对象都可以调用 call 方法。** (对 )

## 二、代码分析题

### 3. 分析以下代码输出

```javascript
var name = "全局";

function showName() {
  console.log(this.name);
}

const obj1 = { name: "对象1" };
const obj2 = { name: "对象2" };

showName();
showName.call(obj1);
showName.apply(obj2);
```

**输出结果：**

全局

对象1

对象2

### 4. 分析以下代码输出

```javascript
function sum(a, b, c) {
  return a + b + c;
}

const result1 = sum.call(null, 1, 2, 3);
const result2 = sum.apply(null, [1, 2, 3]);
const boundSum = sum.bind(null, 1, 2);
const result3 = boundSum(3);

console.log(result1, result2, result3);
```

**输出结果：**

6

6

3

## 三、手写实现题

### 5. 手写 apply 方法

请实现 Function.prototype.myApply 方法：

```javascript
Function.prototype.myApply = function(context, argsArray) {
  // 你的实现代码
    
}
```

### 6. 手写 bind 方法

请实现 Function.prototype.myBind 方法：

```javascript
Function.prototype.myBind = function(context, ...args) {
  // 你的实现代码
}
```

## 四、场景应用题

### 7. 借用数组方法

```javascript
// 如何借用数组的 slice 方法，将类数组 arguments 转为真正的数组？
function convertToArray() {
  // 你的代码
}
```

### 8. 继承实现

```javascript
function Animal(name) {
  this.name = name;
}

Animal.prototype.say = function() {
  console.log(`我是${this.name}`);
};

function Dog(name, breed) {
  // 如何继承 Animal 的构造函数？
  // 你的代码
  this.breed = breed;
}

// 如何继承 Animal 的原型方法？
// 你的代码
```

## 五、进阶思考题

### 9. 多个 bind 调用

```javascript
function test() {
  console.log(this.name);
}

const obj1 = { name: "obj1" };
const obj2 = { name: "obj2" };

const bound1 = test.bind(obj1);
const bound2 = bound1.bind(obj2);

bound2(); // 输出什么？为什么？
```

### 10. new 操作符与 bind

```javascript
function Person(name) {
  this.name = name;
}

const BoundPerson = Person.bind({ name: "绑定对象" });

const p1 = new BoundPerson("张三");
console.log(p1.name); // 输出什么？
```

## 六、错误分析题

### 11. 找出以下代码的问题

```javascript
Function.prototype.myCall = function(context) {
  const fnkey = "__temp__";
  context[fnkey] = this;
  const result = context[fnkey]();
  return result;
};

const obj = { name: "测试" };
function show() {
  console.log(this.name);
}

show.myCall(obj);
```

**问题分析：**

## 参考答案

### 选择题
1.1 D  1.2 D  1.3 C

### 判断题
2.1 ✓  2.2 ✗  2.3 ✗  2.4 ✗

### 代码分析题
3. 输出：全局 对象1 对象2
4. 输出：6 6 6

### 手写实现题
5. apply 实现：
```javascript
Function.prototype.myApply = function(context, argsArray) {
  if (context === null || context === undefined) {
    context = window;
  }
  if (typeof this !== "function") {
    throw new Error("this is not a function");
  }
  const fnkey = Symbol("fn");
  context[fnkey] = this;
  const result = context[fnkey](...(argsArray || []));
  delete context[fnkey];
  return result;
};
```

6. bind 实现：
```javascript
Function.prototype.myBind = function(context, ...args) {
  if (typeof this !== "function") {
    throw new Error("this is not a function");
  }
  const self = this;
  return function(...newArgs) {
    return self.apply(context, [...args, ...newArgs]);
  };
};
```

### 场景应用题
7. 借用数组方法：
```javascript
function convertToArray() {
  return Array.prototype.slice.call(arguments);
}
```

8. 继承实现：
```javascript
function Dog(name, breed) {
  Animal.call(this, name);
  this.breed = breed;
}
Dog.prototype = Object.create(Animal.prototype);
Dog.prototype.constructor = Dog;
```

### 进阶思考题
9. 输出：obj1（bind 绑定后无法再次改变 this）
10. 输出：张三（new 操作符优先级高于 bind）

### 错误分析题
11. 问题：
- 没有处理参数传递
- 使用字符串作为临时属性键，可能覆盖对象原有属性
- 没有进行类型检查
- 没有处理 context 为 null/undefined 的情况