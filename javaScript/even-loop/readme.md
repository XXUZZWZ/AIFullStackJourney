# event loop

事件循环机制(渲染主线程的调度机制)

- js 单线程
  同一时刻只做一件事
  同步任务尽快执行完成,尽快渲染页面(重绘重排)，并处理事件。
  耗时性都任务？
  - settTimeout
  - fetch/ajax
  - eventListener
- script 脚本 一个宏任务

- 微任务是优先级最高的，紧急的。

- 微任务有那些
  - promise.then()
  - MutationObserver 微任务，做到页面渲染前执行
    - dom 改变在页面渲染前拿到 DOM 前端有啥变化
  - queueMicrotask() 浏览器原生的微任务
  - process.nextTick() node 原生微任务

- 多进程

- 操作系统的 调度  cpu 时间轮片法。

- 进程是分配的资源的一个单元。
  - 内存 (地址,物理)，cpu计算机会
  - 独立的进程ID,一定的大小，一定的运行时间
  - 一个程序可能会有多个进程，多个进程之间相互独立。
  - 程序运行以会以进程为单位，进程之间相互独立，互不影响。
  - 进程
    - 每个进程有一个主线程 ，可以创建新的线程，可并发。
    - 管理线程
    - 进程之间可以通信。
      - 主线程
        - 负责执行js代码
        - 线程是进程的子集，线程是进程的资源分配单位。
- 进程间的通信
  - 两个独立进程之间的开销很大
  - 父子进程间通信就很开销小点

- Chrome 浏览器是多进程架构
  - 浏览器主进程
  - 一个tab页就是一个单独的进程
     - 几个进程
     - 安全 crash，一个页面
    - 标签页的渲染主线程
      - 网络线程 fecth /xhr 有专属的下载线程。
      - 计时器线程
      - 事件监听 addEventListener 没有独立的线程？？？？
       但是是由操作系统告诉浏览器进程进程然后，浏览器进程通过IPC通信给渲染进程
      - 渲染线程
      - js单线程
        - 为啥要这样？
           - 简单
           - 线程安全
           - dom编程模型 保证渲染结果唯一，不可以进行线程的争抢。
   - setTimeout 被另一个计时器专属线程，在计时结束后 放到event loop中
    - 到时间了，callback 放入宏任务队列。
    - 放到event loop 队列中
    为什么setTimeout 不准时？
    - 宏任务，微任务，队列

- 渲染进程的主线程
   - 解析HTML树
   - 解析CSS树
   - 结合HTML树和CSS树 生成渲染树
   - 计算样式
   - 生成布局树
   - 分层
   - 生成绘制指令======>给其他相关独立的绘制线程
   - v8引擎 JS 执行
- 事件队列 ？
   - 定时器到点了
   - xhr.onreadystatechange === 4 fetch.then 
   - 页面更新
   - 微任务队列
   - 宏任务队列

JS和渲染是互斥的